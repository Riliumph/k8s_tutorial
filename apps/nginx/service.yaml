apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nginx-namespace
spec:
  type: NodePort
  selector:
    app: nginx-pod-label
  ports:
    - name: web-port
      port: 80
      targetPort: web-port
      nodePort: 30080
