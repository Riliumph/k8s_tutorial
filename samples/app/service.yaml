apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
  namespace: sample-app-namespace
spec:
  type: NodePort
  selector:
    app: sample-app-pod-label
  ports:
    - name: web-port
      port: 80
      targetPort: web-port
      nodePort: 30080
